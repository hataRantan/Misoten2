using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GitReference : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("プルテストのための奴");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
